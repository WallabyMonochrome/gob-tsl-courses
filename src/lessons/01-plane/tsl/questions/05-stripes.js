import { Fn, uv } from 'three/tsl'

import { TODO_PLACEHOLDER } from '../constant.js'

export const thinStripes = Fn(() => {
  const coords = uv()

  return TODO_PLACEHOLDER
})

// const offset = coords.y.mul(PI.mul(2)).WAVES_FREQUENCY().XXX().mul(WAVES_AMPLITUDE())
export const wavyStripes = Fn(() => {
  const coords = uv()

  return TODO_PLACEHOLDER
})
